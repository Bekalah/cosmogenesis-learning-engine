# Codex API

Run `codex_api.py` to serve the expanded codex as a read-only JSON API.

```
python codex_api.py
```

Endpoints:
- `/nodes` – list all nodes
- `/nodes/<id>` – fetch a single node by `node_id`
