-+// p5.js placeholder: actual library should be placed here for offline use.
- 
+/* Minimal p5.js stub for offline demos. */
+export default class p5 {
+  constructor(sketch) {
+    const p = this;
+    p.width = 0;
+    p.height = 0;
+    p.DEGREES = 'degrees';
+    p.createCanvas = function (w, h) {
+      p.width = w;
+      p.height = h;
+      const c = document.createElement('canvas');
+      c.width = w;
+      c.height = h;
+      return {
+        parent(node) {
+          const el = typeof node === 'string' ? document.querySelector(node) : node;
+          if (el) el.appendChild(c);
+          return c;
+        }
+      };
+    };
+    p.angleMode = function () {};
+    p.noLoop = function () {};
+    p.background = function () {};
+    p.translate = function () {};
+    p.rotate = function () {};
+    p.ellipse = function () {};
+    sketch(p);
+  }
+}
